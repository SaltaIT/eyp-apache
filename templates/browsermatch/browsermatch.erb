
<% if defined?(@description) -%>
  #
  # <%= @description %>
  #
<% end -%>
<% if @set.kind_of?(Hash) -%>
  BrowserMatch "<%= @regex %>" <% @set.each do | var, val | %><%= var %>=<%= val %> <% end %>
<% elsif @set.kind_of?(Array) -%>
  BrowserMatch "<%= @regex %>" <% @set.each do | val | %><%= val %> <% end %>
<% end %>
